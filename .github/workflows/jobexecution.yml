name: Job execution

on:
  push:
    branches: [main, patch2]

concurrency:
  group: simple-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Matrix Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        version: [1, 2]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Simulate setup
        run: echo "Setting up version ${{ matrix.version }} on ${{ matrix.os }}"

  #     - name: Cache simulated dependencies
  #       uses: actions/cache@v3
  #       with:
  #         path: my_cache/
  #         key: cache-${{ runner.os }}-v${{ matrix.version }}
  #         restore-keys: |
  #           cache-${{ runner.os }}-

  #     - name: Run tests
  #       run: echo "Running tests for version ${{ matrix.version }} on ${{ matrix.os }}"

  # build:
  #   name: Build Job
  #   needs: test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Simulate build
  #       run: echo "Building the application..."

  #     - name: Create artifact
  #       run: |
  #         mkdir -p output
  #         echo "Build complete" > output/result.txt

  #     - name: Upload artifact
  #       uses: actions/upload-artifact@v3
  #       with:
  #         name: build-result
  #         path: output/

  # deploy-staging:
  #   name: Deploy to Staging
  #   needs: build
  #   if: github.ref == 'refs/heads/develop'
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: staging
  #   steps:
  #     - name: Download build artifact
  #       uses: actions/download-artifact@v3
  #       with:
  #         name: build-result

  #     - name: Deploy
  #       run: echo "Deploying to STAGING"

  # deploy-production:
  #   name: Deploy to Production
  #   needs: build
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: production
  #   steps:
  #     - name: Download build artifact
  #       uses: actions/download-artifact@v3
  #       with:
  #         name: build-result

  #     - name: Deploy
  #       run: echo "Deploying to PRODUCTION"
